services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'pg_isready -U "$$POSTGRES_USER" -d "$$POSTGRES_DB" -h 127.0.0.1',
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  api:
    image: process-management-system-api:latest
    build:
      context: ./api
      dockerfile: Dockerfile
    restart: unless-stopped
    init: true
    env_file:
      - ./.env
    environment:
      DB_HOST: postgres
    expose:
      - '3000'
    volumes:
      - ./api/uploads:/app/uploads
    depends_on:
      postgres:
        condition: service_healthy

  api-lb:
    image: nginx:1.27-alpine
    restart: unless-stopped
    ports:
      - '3000:80'
    volumes:
      - ./docker/api-lb/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      api:
        condition: service_healthy

  web-public:
    image: process-management-system-web-public:latest
    build:
      context: ./web/public
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - '80'
    depends_on:
      api-lb:
        condition: service_started

  public-lb:
    image: nginx:1.27-alpine
    restart: unless-stopped
    ports:
      - '8080:80'
    volumes:
      - ./docker/public-lb/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      web-public:
        condition: service_started

  web-backoffice:
    image: process-management-system-web-backoffice:latest
    build:
      context: ./web/backoffice
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - '80'
    depends_on:
      api-lb:
        condition: service_started

  backoffice-lb:
    image: nginx:1.27-alpine
    restart: unless-stopped
    ports:
      - '8081:80'
    volumes:
      - ./docker/backoffice-lb/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      web-backoffice:
        condition: service_started

volumes:
  pgdata:
